# JeecgBoot Vue3 快速集成 BPMN 流程设计器

## 快速集成方案（30分钟完成）

直接复用 `vite-vue-bpmn-process` 的组件，最小化改动，快速看到效果。

## 第一步：安装依赖（5分钟）

```bash
# 进入 jeecg-vue3 项目根目录
cd jeecgboot-vue3

# 安装 BPMN 相关依赖
pnpm add bpmn-js@^13.0.0
pnpm add camunda-bpmn-moddle@^7.0.1
pnpm add naive-ui@^2.34.4

# 安装类型定义（如果需要）
pnpm add -D @types/bpmn-moddle
```

## 第二步：复制源码文件（10分钟）

### 2.1 下载 vite-vue-bpmn-process 源码
```bash
# 临时下载源码
git clone https://github.com/moon-studio/vite-vue-bpmn-process.git temp-bpmn
```

### 2.2 复制核心文件到 jeecg-vue3 项目

在 `jeecg-vue3/src` 目录下创建以下结构：

```
src/
├── components/
│   └── BpmnProcess/          # 复制整个 vite-vue-bpmn-process/src 目录内容
│       ├── components/       # Designer、Toolbar、Panel 等
│       ├── config/          
│       ├── moddle-extensions/
│       ├── styles/
│       ├── utils/
│       └── types/
└── views/
    └── bpmn/
        └── designer/
            └── index.vue     # 新建包装页面
```

**具体复制命令：**
```bash
# 假设在 jeecg-vue3 项目根目录
# 复制核心组件
cp -r temp-bpmn/src/* src/components/BpmnProcess/

# 清理临时文件
rm -rf temp-bpmn
```

## 第三步：创建包装页面（5分钟）

### 3.1 创建 BPMN 设计器页面

创建文件：`src/views/bpmn/designer/index.vue`

```vue
<template>
  <div class="full-height">
    <!-- 直接使用 BPMN 组件，保持原有样式 -->
    <BpmnProcessDesigner />
  </div>
</template>

<script setup lang="ts">
import { defineAsyncComponent } from 'vue'

// 动态导入 BPMN 组件，避免初始化错误
const BpmnProcessDesigner = defineAsyncComponent(
  () => import('/@/components/BpmnProcess/App.vue')
)
</script>

<script lang="ts">
export default {
  name: 'BpmnDesigner'
}
</script>

<style scoped>
.full-height {
  height: calc(100vh - 180px);
  width: 100%;
  background: #f5f5f5;
}

/* 隔离 naive-ui 样式，避免冲突 */
:deep(.n-config-provider) {
  height: 100%;
}
</style>
```

### 3.2 修改 BPMN 组件的主入口

修改 `src/components/BpmnProcess/App.vue`：

```vue
<template>
  <!-- 使用 naive-ui 的 ConfigProvider 隔离样式 -->
  <n-config-provider :theme="theme">
    <div class="bpmn-app">
      <!-- 原有的 BPMN 组件内容，保持不变 -->
      <Designer />
    </div>
  </n-config-provider>
</template>

<script setup lang="ts">
import { NConfigProvider } from 'naive-ui'
import { computed } from 'vue'
import Designer from './components/Designer/index.vue'

// 简单的主题配置，避免与 ant-design-vue 冲突
const theme = computed(() => null)
</script>

<style>
/* 确保 BPMN 样式优先级 */
.bpmn-app {
  height: 100%;
  width: 100%;
}

/* 导入 BPMN 原有样式 */
@import './styles/index.scss';
</style>
```

## 第四步：配置路由和菜单（10分钟）

### 4.1 添加路由配置

创建文件：`src/router/routes/modules/bpmn.ts`

```typescript
import type { AppRouteModule } from '/@/router/types'

const bpmn: AppRouteModule = {
  path: '/bpmn',
  name: 'Bpmn',
  component: () => import('/@/layouts/default/index.vue'),
  meta: {
    orderNo: 500,
    icon: 'ant-design:apartment-outlined',
    title: '流程管理',
  },
  children: [
    {
      path: 'designer',
      name: 'BpmnDesigner',
      component: () => import('/@/views/bpmn/designer/index.vue'),
      meta: {
        title: '流程设计器',
        icon: 'ant-design:edit-outlined',
      },
    },
  ],
}

export default bpmn
```

### 4.2 注册路由模块

在 `src/router/routes/modules/index.ts` 中添加：

```typescript
// 在文件末尾添加
import bpmn from './bpmn'

export default [
  // ...其他路由
  bpmn,
]
```

### 4.3 配置菜单权限（可选）

如果需要权限控制，在后台管理中添加菜单：

```sql
-- 在数据库中执行（可选）
INSERT INTO `sys_permission` VALUES ('bpmn', NULL, '流程管理', '/bpmn', 'layouts/default/index', NULL, NULL, 1, NULL, NULL, 0, NULL, NULL, 0, NULL, 500, 'ant-design:apartment-outlined', 0, NULL, '2024-01-01 00:00:00', 'admin', '2024-01-01 00:00:00', 'admin', 0, NULL, NULL, 1);

INSERT INTO `sys_permission` VALUES ('bpmn:designer', 'bpmn', '流程设计器', '/bpmn/designer', 'views/bpmn/designer/index', NULL, NULL, 1, NULL, NULL, 0, NULL, NULL, 0, NULL, 1, 'ant-design:edit-outlined', 0, NULL, '2024-01-01 00:00:00', 'admin', '2024-01-01 00:00:00', 'admin', 0, NULL, NULL, 1);
```

## 第五步：处理样式冲突（可选优化）

### 5.1 添加样式隔离

在 `src/components/BpmnProcess/styles/index.scss` 开头添加：

```scss
/* 样式隔离，避免与 ant-design-vue 冲突 */
.bpmn-app {
  /* 重置一些可能冲突的样式 */
  .n-button {
    /* naive-ui 按钮样式 */
  }
  
  .ant-btn {
    /* 确保 ant-design-vue 按钮不受影响 */
    all: revert;
  }
}

/* 原有样式保持不变 */
// 其他原有样式...
```

## 第六步：启动测试

```bash
# 启动项目
pnpm dev

# 访问地址
http://localhost:3100/#/bpmn/designer
```

## 可能遇到的问题及解决方案

### 问题1：依赖冲突
```bash
# 如果遇到版本冲突，使用 --force 强制安装
pnpm add naive-ui --force
```

### 问题2：TypeScript 类型错误
在 `src/types/global.d.ts` 中添加：
```typescript
declare module 'bpmn-js/lib/Modeler'
declare module 'bpmn-js/lib/Viewer'
declare module 'diagram-js/lib/core/Canvas'
```

### 问题3：样式冲突
如果样式冲突严重，可以在包装页面添加：
```vue
<style scoped>
/* 强制隔离样式作用域 */
.full-height :deep(.n-config-provider) {
  isolation: isolate;
}
</style>
```

## 验证结果

完成以上步骤后，您应该能看到：

1. ✅ 左侧菜单出现"流程管理" → "流程设计器"
2. ✅ 点击进入后看到完整的 BPMN 设计器界面
3. ✅ 所有原有功能正常：拖拽、属性编辑、导入导出等
4. ✅ 与 JeecgBoot 的整体风格基本协调

## 后续优化方向

基础功能运行稳定后，可以考虑：

1. **样式统一化**：逐步将 naive-ui 组件替换为 ant-design-vue
2. **功能集成**：添加保存到后台、权限控制等
3. **性能优化**：代码分割、懒加载等

这个方案可以让您在最短时间内看到效果，验证可行性后再进行深度定制。