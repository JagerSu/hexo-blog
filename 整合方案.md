# JeecgBoot Vue3 整合 BPMN 流程设计器方案

## 整合目标
将 vite-vue-bpmn-process 的 BPMN 流程设计功能整合到 jeecg-vue3 项目中，实现工作流程设计能力。

## 技术栈兼容性分析

### 共同点
- ✅ Vue 3.x
- ✅ Vite 构建工具  
- ✅ TypeScript 支持
- ✅ 现代化前端架构

### 差异点
- **UI 框架**: JeecgBoot 使用 Ant-Design-Vue，BPMN 项目使用 NaiveUI
- **样式方案**: JeecgBoot 使用 WindiCSS/UnoCSS，BPMN 项目使用 SCSS
- **状态管理**: JeecgBoot 使用 Pinia，BPMN 项目较简单

## 整合方案一：组件级整合（推荐）

### 1. 依赖安装

```bash
# 进入 jeecg-vue3 项目根目录
cd jeecgboot-vue3

# 安装 BPMN 相关依赖
pnpm add bpmn-js@^13.0.0
pnpm add camunda-bpmn-moddle@^7.0.1
pnpm add diagram-js@^12.0.0

# 如果需要更多扩展
pnpm add bpmn-js-properties-panel
pnpm add bpmn-js-element-templates
```

### 2. 创建 BPMN 模块目录结构

```
src/
├── components/
│   └── Bpmn/                    # BPMN 组件模块
│       ├── Designer/            # 流程设计器
│       │   ├── index.vue
│       │   └── index.ts
│       ├── Palette/             # 工具面板
│       ├── Panel/               # 属性面板  
│       ├── Toolbar/             # 工具栏
│       ├── types/               # TypeScript 类型定义
│       └── utils/               # 工具函数
├── views/
│   └── bpmn/                    # BPMN 页面
│       ├── design/              # 流程设计页面
│       ├── list/                # 流程列表页面
│       └── preview/             # 流程预览页面
```

### 3. 核心组件开发

#### BPMN 设计器组件 
```vue
<!-- src/components/Bpmn/Designer/index.vue -->
<template>
  <div class="bpmn-designer">
    <div class="bpmn-toolbar">
      <BpmnToolbar 
        @import="handleImport"
        @export="handleExport"
        @save="handleSave"
      />
    </div>
    
    <div class="bpmn-container">
      <div class="bpmn-canvas" ref="bpmnCanvas"></div>
      <div class="bpmn-panel">
        <BpmnPanel :element="selectedElement" />
      </div>
    </div>
  </div>
</template>

<script setup lang="ts">
import { ref, onMounted, onUnmounted } from 'vue'
import BpmnModeler from 'bpmn-js/lib/Modeler'
import 'bpmn-js/dist/assets/diagram-js.css'
import 'bpmn-js/dist/assets/bpmn-font/css/bpmn.css'

// JeecgBoot 的消息提示
import { useMessage } from '/@/hooks/web/useMessage'
const { createMessage } = useMessage()

const bpmnCanvas = ref<HTMLElement>()
const selectedElement = ref()
let modeler: BpmnModeler | null = null

onMounted(async () => {
  await initBpmnModeler()
})

const initBpmnModeler = async () => {
  modeler = new BpmnModeler({
    container: bpmnCanvas.value,
    propertiesPanel: {
      parent: '.bpmn-panel'
    }
  })
  
  // 监听元素选择事件
  modeler.on('selection.changed', (event) => {
    selectedElement.value = event.newSelection[0]
  })
}

const handleSave = async () => {
  try {
    const { xml } = await modeler!.saveXML({ format: true })
    // 调用 JeecgBoot 的保存 API
    createMessage.success('保存成功')
    return xml
  } catch (err) {
    createMessage.error('保存失败')
    console.error(err)
  }
}
</script>
```

#### 整合到 JeecgBoot 路由

```typescript
// src/router/routes/modules/bpmn.ts
import type { AppRouteModule } from '/@/router/types'

const bpmn: AppRouteModule = {
  path: '/bpmn',
  name: 'Bpmn',
  component: () => import('/@/layouts/default/index.vue'),
  meta: {
    orderNo: 1000,
    icon: 'ant-design:apartment-outlined',
    title: '流程管理',
  },
  children: [
    {
      path: 'design',
      name: 'BpmnDesign',
      component: () => import('/@/views/bpmn/design/index.vue'),
      meta: {
        title: '流程设计',
        icon: 'ant-design:edit-outlined',
      },
    },
    {
      path: 'list',
      name: 'BpmnList', 
      component: () => import('/@/views/bpmn/list/index.vue'),
      meta: {
        title: '流程列表',
        icon: 'ant-design:unordered-list-outlined',
      },
    }
  ],
}

export default bpmn
```

### 4. 样式适配

创建样式适配文件：

```scss
// src/components/Bpmn/styles/index.scss

/* 适配 Ant Design Vue 主题 */
.bpmn-designer {
  .djs-palette {
    background: var(--background-color);
    border: 1px solid var(--border-color);
  }
  
  .bpp-properties-panel {
    background: var(--component-background);
    border: 1px solid var(--border-color);
    
    .bpp-field-wrapper label {
      color: var(--text-color);
    }
  }
}
```

### 5. API 集成

```typescript
// src/api/bpmn/index.ts
import { defHttp } from '/@/utils/http/axios'

enum Api {
  BPMN_LIST = '/bpmn/list',
  BPMN_SAVE = '/bpmn/save', 
  BPMN_DELETE = '/bpmn/delete',
  BPMN_DEPLOY = '/bpmn/deploy',
}

// 保存流程定义
export const saveBpmnXml = (params: any) => 
  defHttp.post({ url: Api.BPMN_SAVE, params })

// 获取流程列表  
export const getBpmnList = (params: any) =>
  defHttp.get({ url: Api.BPMN_LIST, params })
```

## 整合方案二：微前端整合

如果项目较大，可以考虑微前端方案：

### 1. 使用 qiankun 微前端框架

```bash
# 主应用（JeecgBoot）
pnpm add qiankun

# 子应用（BPMN 设计器）保持独立
```

### 2. 主应用配置

```typescript
// src/micro-app/index.ts
import { registerMicroApps, start } from 'qiankun'

registerMicroApps([
  {
    name: 'bpmn-designer',
    entry: '//localhost:3001', // BPMN 应用地址
    container: '#bpmn-container',
    activeRule: '/bpmn',
  },
])

start()
```

## 推荐实施步骤

### 第一阶段：基础整合
1. ✅ 安装 BPMN 相关依赖
2. ✅ 创建基础的 BPMN 设计器组件
3. ✅ 整合到 JeecgBoot 路由系统
4. ✅ 样式适配和主题统一

### 第二阶段：功能完善
1. 🔄 实现流程的保存、加载、预览
2. 🔄 整合权限控制系统
3. 🔄 添加流程版本管理
4. 🔄 实现流程部署功能

### 第三阶段：高级功能
1. ⏳ 流程执行跟踪
2. ⏳ 表单设计器集成
3. ⏳ 流程报表统计
4. ⏳ 移动端适配

## 注意事项

1. **样式冲突**: 注意处理 Ant Design Vue 和 BPMN.js 的样式冲突
2. **TypeScript 类型**: 确保 BPMN 相关的 TypeScript 类型定义正确
3. **打包优化**: BPMN.js 较大，注意代码分割和懒加载
4. **浏览器兼容**: 确保目标浏览器支持 BPMN.js

## 预期效果

整合完成后，您将拥有：
- ✨ 完整的企业级管理后台（JeecgBoot）
- ✨ 专业的流程设计能力（BPMN）
- ✨ 统一的用户体验和权限控制
- ✨ 可扩展的工作流引擎基础